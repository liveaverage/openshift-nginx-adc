---
apiVersion: v1

# The repository name in registry1, excluding /ironbank/
name: "f5-networks/nginx-plus-software-adc"

# List of tags to push for the repository in registry1
# The most specific version should be the first tag and will be shown
# on ironbank.dso.mil
tags:
- "1.23.1"
- "latest"

# Build args passed to Dockerfile ARGs
args:
  BASE_IMAGE: "ironbank/redhat/ubi/ubi8"
  BASE_TAG: "8.4"

# Docker image labels
labels:
  # Name of the image
  org.opencontainers.image.title: "nplus-sw-adc"
  # Human-readable description of the software packaged in the image
  org.opencontainers.image.description: "NGINX Plus Micro Services Platform"
  # License(s) under which contained software is distributed
  org.opencontainers.image.licenses: "nginx-eula-License"
  # URL to find more information on the image
  org.opencontainers.image.url: "https://docs.nginx.com/nginx/admin-guide/installing-nginx/installing-nginx-plus/"
  # Name of the distributing entity, organization or individual
  org.opencontainers.image.vendor: "F5-NIGNX-Plus"
  # Authoritative version of the software
  org.opencontainers.image.version: "1.23.1"
  # Keywords to help with search (ex. "cicd,gitops,golang")
  mil.dso.ironbank.image.keywords: "k8s,sw,adc"
  # This value can be "opensource" or "commercial"
  mil.dso.ironbank.image.type: "commercial"
  # Product the image belongs to for grouping multiple images
  mil.dso.ironbank.product.name: "f5-networks/nginx-plus-software-adc"

# List of resources to make available to the offline build context
#resources:
#  - url: "wget https://raw.githubusercontent.com/nginxinc/nginx-plus-install-tools/main/ngxunprivinst.sh"
#    filename: "ngxunprivinst.sh" # [required field] desired staging name for the build context
#    validation:
#      type: "sha256" # supported: sha256, sha512
#      value: "3d6b4cfca92067edd5c860c212ff5153d1e162b8791408bc671900309eb555ec" # must be lowercase
    # if the file you pull is from a github repo, make sure this is the official repo for that file,
    # and indicate that in a comment in this file

# List of project maintainers
maintainers:
- email: "d.turner@f5.com"
  # The name of the current container owner
  name: "Douglas Turner"
  # The gitlab username of the current container owner
  username: "dturnerf5com"
